def missing(full_set, missing_set):
    total = 0
    for num in full_set:
        total ^= num
    for num in missing_set:
        total ^= num
    return total

print(missing([1,2,3,4], [4,3,2])) #1
